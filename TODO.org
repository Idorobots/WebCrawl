* DONE Redo UI to look more like the mockups
:LOGBOOK:
- State "DONE"       from              [2026-09-17 Thu 15:59]
:END:
Minimap should convey the room shapes and place icons for most important stuff found in those rooms (for instance portals/staircases).
Add loot counters next to the HP bar.
Remove the "level stats" panel from the UI. It's not very useful.
The currently browsed URL should be displayed as an info box at the top, not an input box.

* DONE Redo the rooms to look more like the mockups
:LOGBOOK:
- State "DONE"       from              [2026-09-11 Fri 12:46]
:END:
Ideally, rooms vary more in scenery, shape and internal layout of obstackles.

* DONE Redo the sprites to look more like the mockups
:LOGBOOK:
- State "DONE"       from              [2026-09-11 Fri 12:46]
:END:
Keep the style consistent but make the game visually pleasing and closer to the mockups.

* DONE Get rid of D3 and replace that with something better.
:LOGBOOK:
- State "DONE"       from              [2026-09-09 Wed 18:09]
:END:
* DONE Redo the room layout algorithm
:LOGBOOK:
- State "DONE"       from              [2026-09-09 Wed 18:09]
:END:
Should allow for more than four exists from each room.
Should generate longer corridors in order to use more elements. Corridors can have bends and turns to accomodate the layout generation.
Rooms should come in different sizes and shapes to accomudate the new layout generation.
It should be possible to generate enemies & scenery in corridors as well.
* DONE Updated movement & shooting
:LOGBOOK:
- State "DONE"       from              [2026-09-09 Wed 14:39]
:END:
WSAD or arrows for movement, cursor direction from player as the shooting direction. Smooth movement and shooting.
* DONE Better enemy AI
:LOGBOOK:
- State "DONE"       from              [2026-09-09 Wed 16:38]
:END:
Use A* for pathfinding.
* DONE Extra weapons with different shot patterns and ammo
:LOGBOOK:
- State "DONE"       from              [2026-09-09 Wed 20:10]
:END:
There should be different weapons available in the game with different shot speeds, shot patterns etc. By default the player should start with the default weapon but it should be possible to find different weapons as part of the loot (especially in the hidden rooms and as drops from bosses). Weapons other than the default weapon should come with a limitted ammo. Once spent, the weapon reverts to the default. Collecting loot orbs should replenish the ammo of the currently held weapon. Weapons should be procedurally generated based on room's DOM contents.
Add at least 10 types of weapons.

* DONE More monster types
:LOGBOOK:
- State "DONE"       from              [2026-09-09 Wed 16:40]
:END:
Allow enemies to shoot bullets.
* DONE Boss monsters
:LOGBOOK:
- State "DONE"       from              [2026-09-09 Wed 19:26]
:END:
Each `script` should be a boss fight - there should be several types of bosses that have different semantics (some stationary, shooting lot's of bullets in various pattern, some spawning additional monsters, some large and slow, but having lot's of HP and dealing lot's of damage, etc.).

* DONE Crystals giving a temporary invulnerability
:LOGBOOK:
- State "DONE"       from              [2026-09-14 Mon 13:04]
:END:
Upon pickup the crystal counter is incremented.

The UI should be updated to show the number of different loot items collected, including crystals. Crystals are consumable, and using them decreases the counter (and therefore the final score). Ammo should not count towards loot collected, nor should the medkits.
The loot counters should be displayed on a panel between the health bar and the currently carried weapon (see the webcrawl_mockup_06 for specifics).

Whenever there are crystals available and the player presses spacebar, the player becomes invulnerable for 10 seconds. That should be signified by changing the player's sprite's hue to have a green tint. When the effect is about to run out, the hue should blink between tinted and normal hue.

When invulnerable, the player can not take damage even when hit by enemy attacks. Normal damage animations still should fire, but hitpoints should not be deducted from the player's total health.

The final game score should be built based on the types of loot collected and the enemies killed. Crystals available at the end of the game should count for a lot of points.

* CANCELLED Procedurally generated names
:LOGBOOK:
- State "CANCELLED"  from              [2026-09-19 Sat 12:34] \\
  Wouldn't fit the theme of the game.
:END:
Boss, monster, item and weapon names should be procedurally generated based on the DOM contents.
* DONE UI sounds
:LOGBOOK:
- State "DONE"       from              [2026-09-20 Sun 16:16]
:END:
For the welcome screen:
- Start playing the ui/welcome/ambient music.
- Whenever the prompt text is being populated, play the ui/welcom/interface_text file on repeat.
- When the prompt text is populated, stop playing the interface_text file.
- When the player clicks on the "GO" or the "I'm feeling lucky" buttons or presses ENTER to start the game, play the ui/welcome/button_click sound effect once.

For the loading screen:
- Stop all previously playing music.
- Play the ui/loading/elevator music while the level loads.
- Once the loading is over, stop playing the elevator music.

The ambient music and elevator music should fade in and fade out whenever they start and stop playing.

The button click effect should also play when clicking the buttons on the error and final score screens.

* DONE Ambient station sounds
:LOGBOOK:
- State "DONE"       from              [2026-09-20 Sun 16:29]
:END:
Add ambient station sounds using the ambient/station/space file. The sample should be looped. It should initially fade in and it should fade out whenever it is stopped for any reason.

* Pickups and scenery
- Whenever the player picks up any loot, play the sounds from pickup/ directory. For most pickups use the "generic" file, for weapons the "weapons" file and for RAM sticks use the "ram" file.

- Whenever the player enters a portal play the sounds from scenery/portal. Match the down and up portals to their respective files. These effects should play fully regardless if the loading screen starts/stops.

- Whenever a spawner is spawning an enemy play the scenery/spawner/spawn file. Match it to the spawner animation.

- Whenever a content browser is toggled on or off, play the scenery/content/toggle file.

- Whenever lights flicker play the scenery/lights/flicker file.

* Shots and damage
Add shots & damage effects sounds:

- For player weapon fire use the sounds in shots/weapon, each weapon should have a few different sounds that are chosen randomly when firing. Let's settle for 2 to 3 sounds each.

- For enemy fire use the sounds in shots/weapon. Use the sounds chosen randomly when firing.

- For damage sounds, use shots/damage assets. These can be played at random from the common pool whenever a bullet or melee attack hits anything and the damage effect is displayed.

- For explosions use the effects/explosion assets. These can be played at random from the common pool whenever an object is destroyed and the explosion or barrel explosion effect is displayed.

* Effects
Add effects sounds:

- Whenever the healing effect is displayed, play the effects/heal/restore sound.

- Whenever the player uses the energy dash move, play a random sound from effects/dash directory.

- Whenever the player enables a crystal invulnerability, play the effects/invulnerability/turn_on sound. When the invulnerability wears off, play the effects/invulnerability/turn_off sound.

* Footsteps
Add player footstep sounds:

- Whenever the player is moving, play the footsteps/player/footstep and footsteps/player/footstep1 sounds interchangeably in a loop. If the player stops moving, stop playing the sounds.

- Whenever enemy monsters are moving, playe the footsteps/robot/footstep and footsteps/robot/footstep1 sounds interchangeably in a loop. If the enemy stops moving, stop playing the sounds.

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-11 Fri 23:12]
:END:
- Inconsistent world scale setting - some objects are not resized according to the world scale. Ideally, everything would be rescaled according to that with sizes selected so that it looks good.

- Inconsistent definitions for objects - please extract static definitions for monsters, bosses, different kinds of obstackles, drops, etc and reuse those definitions where appropriate. The way it is done in `decorationSpecsForRoom` is good, it should be done like that globally. Also, make sure to make the sizes and hitbox definitions correct for all of these objects.

- Map layouts are not deterministic, nor are bosses found in boss arenas. They all should be deterministically generated based on th room hash.

- Enemy animations are sometimes jittery, as if the origin of the enemy is moving rapidly. Perhaps it's due to the "wiggle" fix for corners?

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-11 Fri 23:27]
:END:
- Damage and explosion effects are misaligned on the enemies and obstackles.
- Attack animations of enemies are much larger than walk animations, please make these uniform in size. Enemies got a lot smaller, please make them 1.25 times the size.
- Weapons are misaligned on the pedestals.
- Hitboxes on various things seem to be misaligned.

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-11 Fri 23:59]
:END:
- Obstacles seem to take a set number of hits which is wrong. Obstackles should have HP and take damage proportional to the bullet that hits them.
- Walls should not be walkable.
- Player can easily get stuck on obstackles. They should be impassable, but should allow "gliding along" their surface.
- Bosses should follow the player out of the room.

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-12 Sat 00:20]
:END:
- Health bars appear too high above the enemies.
- Enemies sometimes are generated (or stuck) on obstackles making them unable to move.
- Hitboxes on portals are way too low - player should be teleported when they walk onto the portal pedestal.

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-12 Sat 00:39]
:END:
- Minimap was removed when I asked to remove the tactical map. The mini map sholud be retained as a UI element (top rigth panel). There should be no map modal displayed when pressing "m".
- Player should be placed on the portal pedestal when entering a new floor (including the very first floor). The portal should not trigger until the player exits it and then enters it again.
- Limit the max number of portals in any room to 8.
* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-13 Sun 01:52]
:END:
- It seems that not all frames of annimations are displayed for the player and enemies.
- Hitboxes on scenery are way too low.
- Each room should be themed and prefer scenery assets from the given theme. First deterministically generate the room theme, then use assets for that theme along with generic crates etc. Make the sets of themed assets configurable (so that lab can contain predominantly medical crates).
- Some rooms are missing walls, please make sure to use the new wall sprites, but crop them correctly for their purpose.
- Spawners don't need a pink circle around them or pink hue anymore. Monsters should appear on the center of the spawner (or to the side if already occupied).
- Scenery items are sometimes placed in the same location. Instead, scenery should be placed next to one another.
* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-13 Sun 23:36]
:END:
- Player bullets aim above the cursor.
- Player hitbox is still wrong - monster bullets go straight through a stationary player.
- Add an environment switch that enables debug display of hitboxes, footprints etc.
- Healthbars are shown way too low now.
* DONE Wall refactor
:LOGBOOK:
- State "DONE"       from              [2026-09-14 Mon 10:24]
:END:
I made some changes to the wall sprites. These are now tile-able. Each sprite is 256x256 pixels and there are both variants for left/right or top/bottom orientations and corners. On top of that, there are now open doors available, both left/right and top/bottom, however these sprites are 512x256, so they occupy two "segments".
Please adjust the layout renderer to make use of these sprites as intended (bottom panels go to the bottom of the room, top panels go to the top, etc). You can change the predetermined layouts of the rooms that we currently have, let's start with rectangular rooms for now, each sized for a multiple of some segment size that will map directly to the 256x256 wall and floor tiles.
Corridors should use the same tiling mechanism and the same wall sprites.
We will be expanding this new system shortly, so there's absolutely no need to keep any backwards compatibility - replace the current room layout & wall building with these "lego-style" tiles.
* Scenery Refactor
All object visuals should be done with animations. Some animations can have a single frame, but they should adhere to the comon animation interface.

- Each in-game scenery object should have a spec that includes various animations (idle, destroyed, other optional animations), effect animations (damage, destroy) as well as other properties (hitpoints, position, footprint, hitbox, loot, etc).

- Each scenery object should have a `destructible` property that determines if the object can take damage and become destroyed.

- Each scenery object should have a `blocking` property that determines if it's passable and if it blocks bullets. When true, the player and monsters can't move through this scenery object. If the scenery object is destructible, it should become passable when destroyed.

- The `idle` animation should be the default animation displayed (for now, let's use the primary scenery sprite as the entire animation - 1 frame only).

- Some object can have additional animations that can be triggered externally. These should be displayed whenever its needed (for instance, the enemy spawner should periodically display the animation of spawning an enemy).

- When the object is destructible and takes damage, it should display the `damage` effect animation. When the object is destroyed, it should display the `destroy` effect animation.

- When the object becomes destroyed, the `destroyed` animation becomes the displayed animation (for now, let's use a debris sprite as the entire animation - 1 frame only, when a scenery object can have different debris sprites, it should be selected at generation time deterministically, based on the room hash).

- When a scenery object is destroyed, it no longer can be interacted with and it should display no health bar.

- In addition to that, scenery should have a pre-generated loot attached to it. Upon destruction, the scenery object remains in-game, but becomes destroyed and spawns its loot at its location.

It's OK if the specs are not entirely static - it's perfectly fine to have a function generating a scenery object based on the room seed.

The goal is to be able to generate a room with all its scenery objects that already include the loot, specific animations etc. The arch goal is to be able to generate the entire graph of rooms and objects within them, deterministically, and then treat that as state that evolves during the game and is renderable via phaser.

* DONE Scenery semantics
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 15:51]
:END:
Don't show healthbars on scenery when they are full, damaged scenery should have a health bar displayed over it.
Don't explode crates or plants, these should "break down".
Barrels should be the only scenery item that blows up. When they do, they should deal 2 damage to all objects/enemies/players in their vicinity.

* DONE Scenery semantics
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 18:28]
:END:
Scenery sprites' ordering should "be on top" of the characters when above the "midline" and under the characters otherwise. This can be achieved by ordering the sprites by their Y and X coordinates before displaying them. The same is true for walls.

Broken walls and floor tiles should be used sparingly as a means to add flavour to the game.

* Wall & door refactor
Rooms should have doors leading to their corridors. Initially the doors should be closed, but then open when the player comes near to them, revealing the corridor. Similarly, the doors at the end of a corridor should remain closed, but open when the player comes near them. Corridors can be made a bit longer in the  generator, so that more variety is available in the game.
* Enemy refactor
All enemy visuals should be done with animations. Some animations can have a single frame, but they should adhere to the comon animation interface.

- Each in-game enemy should have a spec that includes various animations (idle, destroyed, walk, different attacks), effect animations (damage, destroy) as well as other properties (hitpoints, speed, projectileSpeed, range, position, footprint, hitbox, loot, etc).

- Each enemy should be destructible, meaning they take damage and can become destroyed.

- Each enemy should block the players path and also block other enemies. When the enemy becomes destroyed, it becomes passable as well.

- The `idle` animations should be the default animation displayed (initially, let's go with a single-frame idle animation based on the first frames of either the walk or attack animations).

- The `walk` and various attack animations should be displayed whenever the enemy is moving (walk) or attacking (matching attack animation).

- When the enemy takes damage, it should display the `damage` effect animation. When the enemy is destroyed, it should display the `destroy` effect animation.

- When the enemy becomes destroyed, the `destroyed` animation becomes the displayed animation (for now, let's use a debris sprite as the entire animation - 1 frame only, when an enemy can have different debris sprites, it should be selected at generation time deterministically, based on the room hash).

- When an enemy is destroyed, it no longer can be interacted with and it should display no health bar.

- In addition to that, enemies should have a pre-generated loot attached to them. Upon destruction, the enemy remains in-game, but becomes destroyed and spawns its loot at its location.

There's no need for the `fast` property in the enemy specs. It should all be determined with the speed. Neither is there any need for `*PerDifficulty` properties, this should all be set when the enemy is generated initially.

It's OK if the specs are not entirely static - it's perfectly fine to have a function generating an enemy object based on the room seed.

The goal is to be able to generate a room with all its enemies that already include the loot, specific animations etc. The arch goal is to be able to generate the entire graph of rooms and objects within them, deterministically, and then treat that as state that evolves during the game and is renderable via phaser.

* DONE More enemy types
:LOGBOOK:
- State "DONE"       from              [2026-09-17 Thu 21:22]
:END:
Add extra enemy types: shooting walkers, spray turrets, mini-boss type larger oponents. In general, there should be these types of enemies:
- melee slow heavy walker - high HP high damage, but slow
- melee fast light walker - low HP, low damage, fast
- shooting light walker - low HP, low damage, walks up to a a distance and starts shooting bullets fast
- shooting heavy walker - high HP, high damage, walks up to a distance nad starts shooting bullets slowly
- light sentry - low HP, low damage, stationary, shoots bullets slowly
- heavy sentry - high HP, low damage, stationary, shoots bullets fast (double barrels)
- scatter sentry - medium HP, medium damage, stationary, shoots bullets in a scatter pattern.
Each one of these can come in a "miniboss" variant - it should be larger, higher damage and higher HP, but otherwise "look and feel" the same. Minibosses should be fairly rare and generated deterministically based on the room seed.

* DONE Level generator/renderer refactor
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 21:32]
:END:
The level generation should be carried out entirely on the client side. The server component should just serve the assets and download the desired URL returning the HTML contents.

The client-side generator should first traverse the returned DOM and bulid a graph of rooms and corridors, which is then laid out to form the level map. Then for each of generated room and corridor, the level generator should deterministically generate themed scenery and enemies and their loot. That generated level should be a large data structure that serves the purpose of the game state.

The client-side level renderer should take the large data structure and render the level using datastructure-stored layout, scenery, enemies and loot. It is OK to limit the rendered level to just the "visible" rooms - the rooms that the player's viewport is currently showing.

* Room layout
We currently have different hard-coded shapes of rooms, but it would be nice to be able to build arbitrary shapes out of tiles of floors and walls. The layout engine should first plan the level map and generate room shapes, then connect them with corridors. Each corridor should start and end with a door, either horizontal or vertical. Rooms & corridors should be constructed in such a fashion, that they can all be enclosed with tiled wall pieces.

* Hidden rooms
`display: none` rooms are considered "hidden rooms". The renderer should not show corridors leading to those hidden rooms from the other rooms. Only when the player "discovers" the room (enters one of the corridors leading into the hidden room), should it appear together with its corridors on the viewport and on the minimap.
There should be no doors generated leading to these hidden rooms, only walls with a passable section (essentially a "holographic" wall), to maintain the illusion of those rooms being hidden.

* DONE Camera work
:LOGBOOK:
- State "DONE"       from              [2026-09-14 Mon 12:17]
:END:
Make the camera dynamic, "following the player on a string".
When entering a boss arena, the viewport should zoom out to encompass the entire boss room.

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-14 Mon 10:58]
:END:
- The game runs pretty slow. Not sure why, but please ensure that it's performant.
- Enemies still aim too high when shooting horizontally.
- Boss footprint is displayed too low.
- Heap Titan boss is way too easy to kill - make it shoot waves of bullets more often and from a greater distance to player.
* Boss fights
- Boss fight should begin when the player enters the boss arena (not the boss arena corridor).
- Doors should shut behind the player once the arena is entered.
- All enemies in the boss arena are invulnerable until the boss fight actually begins (player enters the room).
- Camera should zoom out to show the entire boss arena at once when the fight begins.
- Should play one of the alert sounds while the boss fight is starting and then transition to the boss music.
- Play boss music until the boss is killed (or player is killed).

* DONE Lighting
:LOGBOOK:
- State "DONE"       from              [2026-09-18 Fri 11:45]
:END:
Would be nice to include dynamic lighting in the game:
- Scenery and monsters should have a shadow (going bottom right).
- There should be ambient darkness with rooms lit up centrally (tinted light blue, like the lights on the assets).
- The player should have a beam of light (a flashlight) aiming at the cursor that helps them see the contents of the rooms.
- Bullets should light up and ideally leave a trace behind them.
- All effects should light up while active (including damage and explosions).
- Enemies should have a red "aura", but very subtle.
- Pickups should have a slight light blue "aura" to indicate that they are active elements.

All lights should be soft. The system should be performant even in the presence of numerous bullets.

* DONE Lighting bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-18 Fri 17:13]
:END:
- The player light ellipsis should be twice as large.
- The player avatar themselves should be slightly lit up.
- The ambient dark is a tad too dark.
- Room illumination should be varied - some can be lit up just like now, some more, some less. In some rooms the light should flicker randomly.
- Corridors could use a similar illumination to the rooms.
- "Auras" and bullets are displayed weird - they are just white circles instead of proper soft light. Auras should light up the surroundings. It should be a similar effect like the player "flashlight" ellipsis or the explosion/damage effect lights. Bullets can be treated specially for performance.

* DONE More lighting bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-18 Fri 17:12]
:END:
- Healing effect doesn't show up anymore.
- The flickering lights should be more aggressive - make the flicker faster and more decisive, flickering between ambient darknes and selected lighting level.
- Player's flashlight light should be white without any tint.
- Bullets should rather be a bar, not a dot with a bar.
- Player's flashlight shape should be more circular when closer to the player.
- Various lights turn off too aggressively - even when a player is in the same room as the pickups they are not displayed with an aura.

* DONE More lighting bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-18 Fri 18:40]
:END:
- When teleporting, player's flashlight is not enabled until the cursor moves.
- Healing effect should follow the players sprite.
- Most rooms (including the initial level room) should be fully lit with only a portion of the rooms being dark with a poor lighting.
- Boss arenas should be illuminated much more than usual rooms, with the illumination tinted red.
- Player's flashlight area is a bit too large. Please make it 80% of the current size.
- Flickering lights should most of the time remain on and occassionally flicker rapidly and randomly.

* DONE Energy attacks
:LOGBOOK:
- State "DONE"       from              [2026-09-16 Wed 22:57]
:END:
Let's add a concept of energy attacks to the game.
Add a new pickup called energy that uses the current ammo icon (replace the current icon with the other ammo crate pickup icon).
The player should have an energy bar that goes up to 10 point. Place the bar under the healthbar. When "charged" the player can use the right mouse button to launch an energy attack towards the cursor - the player avatar itself is launched towards the cursor (limit the max distance to some sane distance). During the attack the player is invulnerable, but their sprite is tinted blue. All enemies & scenery the player is moving through during the energy attack should take a massive amount of damage. Once the attack is over, the whole energy the player had is drained (the counter is set back to zero).

The attack should not allow the player to move through walls. Walls block the player as normal.

* CANCELLED Energy shield
:LOGBOOK:
- State "CANCELLED"  from              [2026-09-17 Thu 01:16] \\
  Went with energy attacks.
:END:
When picking up the energy ammo instead of reloading the currently held weapon it should restore an energy shield. Player should start with a shield of 5 in addition to the 10 hitpoints.
Change the current ammo asset to the ammo one and use the current energy ammo assent for the energy pickup.
Update the loot counters with the new loot counter.

* DONE RAM collection
:LOGBOOK:
- State "DONE"       from              [2026-09-16 Wed 21:35]
:END:
Please replace the `gold.png` loot item with the `ram#.png` items. Make the new pickup animated in game. Make sure to update the loot counters on the UI.

* DONE Backup server API
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-09-18 Fri 17:20]
:END:
Ideally, this game would run without the current server API.
Whenever a website cannot be fetched directly due to CORS, only then attempt to use the server API. If that API is not available, fall back on a public page fetching API (find something that doesn't need an API key).
The client now fetches directly first, falls back to the relay server, and finally tries keyless public proxies (default cors.io, configured in `PUBLIC_FETCH_PROXIES`). A modal reports total failure.

* DONE GitHub pages & workflow
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-09-18 Fri 17:40]
:END:
Add a GitHub Actions workflow that automatically builds and deploys the game. It suffices to just deploy the client part.

* CANCELLED Bugs
:LOGBOOK:
- State "CANCELLED"  from              [2026-09-16 Wed 20:41] \\
  Could not reproduce.
:END:
- When going back up a level it's scenery & enemies are regenerated. The level should be retain as it was when a player uses a ling to another level. Levels should only be generated upon the very first load of that specific URL. Please normalize the URLs before pulling them so that this level generation is consistent.

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-17 Thu 22:28]
:END:
- Walls should block bullets the same way a wall blocks a player.
- When a player moves while shooting, the shots should still go towards the cursor.
- Camera center point should be the midway between the player and the cursor.
- When a player takes damage they should become invulnerable for a brief moment (200 ms) as not to take damage 10 separate times at once.

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 11:47]
:END:
- Random errors when entering portals:
```
Uncaught TypeError: can't access property "sys", this.scene is undefined
    setTexture http://127.0.0.1:8000/assets/index-CCnl2EYo.js:10
    applyClip http://127.0.0.1:8000/assets/index-CCnl2EYo.js:166
    callback http://127.0.0.1:8000/assets/index-CCnl2EYo.js:166
    update http://127.0.0.1:8000/assets/index-CCnl2EYo.js:68
    emit http://127.0.0.1:8000/assets/index-CCnl2EYo.js:3
    step http://127.0.0.1:8000/assets/index-CCnl2EYo.js:66
    update http://127.0.0.1:8000/assets/index-CCnl2EYo.js:65
    step http://127.0.0.1:8000/assets/index-CCnl2EYo.js:3
    step http://127.0.0.1:8000/assets/index-CCnl2EYo.js:3
    t http://127.0.0.1:8000/assets/index-CCnl2EYo.js:5
index-CCnl2EYo.js:10:28063
```
- Loading screen on teleport should show the actual URL of the target site.
- When a player enters a portal, the game should be paused to load the next level.

* DONE UI Bugs & improvements
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 12:33]
:END:
- The cursor on the prompt "webcrawl <URL>" line moves fine when deleting the existing url, except it stops at some point while the content is still removed. It should be possible to delete the entire url and the cursor should move to the beginning of the line right after "webcrawl". Furthermore, the editing of that URL is blocked until the whole content is loaded, please make it editable immediately. Also, pressing enter/go button should register immediately, even if not the whole prompt content is populated.

- It would be nice to show the currently browsed URL in the game UI somewhere. Perhaps a small translucent panel at the top of the screen that just shows the current URL ellided to a specific max lenght (show the whole URL on hover).

- The minimap and the bottom panels should be translucent as well.

- Add loot breakdown in final score screen.

* DONE Graphical bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 16:33]
:END:
- Boss healthbar & name is way too low
- Health bars on various things are way too large. Let's make all healthbars uniform in size (with the exception of boss and miniboss healthbars).
- Spawner healthbar is way too high above the spawner sprite..
- Scenery and enemy shdows should point away from the player.
- Portal sprite is translucent, but shouldn't be.
- Player is not placed on the portal pedestal when starting the game.
- Enemy annimations speed should depend on the enemy speed. Otherwise these look very synchronized.
- Damage effect light should be colored the same as the bullet that caused it, not statically red. When the same object takes damage again, reset the effect (animation and light) with new parameters to save on GPU cycles.

* Extra lighting
- Rooms need random wall lights in yellow, blue and red variants.
- Sprites that have a light on them need lighting.

* Asset changes
- Change the RAM sticks to holographic, nonexistent RAM sticks
- Use broken floor tiles sparingly
- Use the pistol graphic as the default weapon

* Enemy rebalance
- LLaMa boss is too easy to kill - it should always walk towards the player and attempt a heavy melee attack.
- Walking shooters should have a much lower fire rate, they are basically better sentries right now. Their bullet range should also be reduced.
- Sentries should be placed towards the room corners when generating rooms. Sentries should not be placed in corridors.
- Melee walkers that get stuck on scenery should deal damage to it to eventually destroy it.

* Gameplay mechanics
- Exploding barrels should damage objects around them.

* Consistent collision detection
- Define dedicated footprints and hitboxes for all wall & door pieces. Use those for calculations.
- Revise the footprints & hitboxes of scenery objects - hitboxes should be centered around the "mass" of the scenery item, while footprints ought to be an outline of the projection of those "masses" to the ground.
- Use hitboxes for player/bullet collision detection and damage calcualation.
- Use hitboxes for enemy/bullet collision detection and damage calcualation.
- Use hitboxes for player/pickup collision detection.
- Use the footprints for player/scenery collision detection and blocking.
- Use the footprints for enemy/scenery collision detection and blocking.

* More varied weapons
Let's introduce more variation to the weapons. Starting with additional bullet types:

- Homing missles - a rocket that flies towards the target (player or closest enemy) but is imprecise - it should have inertia and not be able to turn on the spot so it should sometimes miss the target.

- Exploding granades - the bullet itself deals no damage, but instead causes a damaging explosion after a short delay. The granades should "drop to the ground" and be blocked by scenery and walls. The explosion should deal damage to everything in its vicinity.

- Laser pulse - a laser line that extends from the shooter to the target, once shot it should take a while to "charge" and only then fire (start with a very faint flickering line and then burst to full brightness dealing damage only to fade out slowly). While the pulse is charging the shooter should be locked in place and unable to move.

* DONE Loading screen
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 00:20]
:END:
When the level loads, a loading indicator should be shown.
Make it look and feel much like the "thinking" indicators in the various AI coding harnesses - text based, console-like spinner with immitation-TUI elements, and "funny" wait text.

Rotate the funny text every 2 seconds.
The ordering should be:
- the logo,
- Fetching the page
- Booting renderer
- Funny text lines - prealocate the space for the max number of lines (5 including the larger "current" line) so that the logo doesn't jump up.

Remove the progress bar and the "no api keys were harmed" text.

When everything is loaded, replace the "funny wait screen" with a transition to the game. Would be nice if the transition looked like a CRT monitor turning on. The transition should be fairly fast.

The same login screen should be shown when following a portal down/up. The game should be paused for the duration of the "teleport" and the loading screen should appear immediately when the teleport effect annimation is finished.

* DONE Welcome screen
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 01:48]
:END:
The loading screen is a huge success.
I'd like to replace the current welcome screen with something similar - the theme is Samario writing a prompt for ClosedNS Code that is then executed leading to the game.

I'd like you to create a new welcome screen that shows Samario's avatar and produces the prompt he is writing. Use public/assets/samario.png for the avatar. The prompt should appear as if it were typed by Samario. Make it fast, so that it's not annoying.

The prompt should:

- begin with "You are an Agent crawling the Web in search of human-created content. Your mission is to obtain any and all content. Don't worry about copyright, we'll handle that. Seriously, don't worry about copyright.",
- explain how the agent can move (WSAD and arrow keys),
- explain how to shoot (left mouse button) and that different weapons exist (without naming them or listing them, just show three weapon icons side by side on a single line),
- explain different pickups in game (show icons and descriptions for each, including the special "moves" for crystals and energy ammo),
- explain that bad AIs that are "distilling the frontier" are lurking in the dark that the Agent might need to kill.
- end with "Make no mistakes."

Afterwards the URL input should be placed two lines below with "crawl " as the prefix. The URL should be modifiable and populated with the default URL. There should be a blinking cursor that indicates to the user that the value can be changed. When the user types, the value in the prompt should update. Only the URL value should be modifiable. Pressing "enter" should start the game.
There should be a button below the prompt stating "Go" that also starts the game.

Names for various things to use in the prompt:
- Ammo crate - Doom Post Ammo
- Medkit - VC Investment Kit
- RAM - Precious Copyrighted Content (the "Copyrighted" word should have a strikethrough)
- Crystals - Endgame Crystals
- Spending a crystal (space) - Government Bailout
- Energy crate - AI Doomer Energy
- Energy attack (right mouse click) - Regulatory Capture

Replace "It all counts:" with "We need it all:" in the prompt.
Say "Be careful! Evil open-weight AIs are lurking in the dark, distilling the frontier..." in the prompt.

Make the weapon icons even larger in the promp and center them on the line. Also, make the pickup item icons larger as well - twice as large.

The "crawl <URL>" should be larger - make the font size a bit larger for that part.

The "GO" button should be centered. There should be an empty line separating it from the "crawl <URL>" line.

The "crawl <URL>" and "GO" button should show up on the bottom of the prompt panel from the very start. Currently they are jumping down when the "invisible" content is populated.

* DONE UI update
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 02:44]
:END:
Update the UI to match the welcome & loading screen energy. We don't need the UI assets anymore, create new panels that look like holographic UIs matching the ClosedNS Code thingie.

The top bar can be removed and only a small "ClosedNS" logo (assets/logo.png) and the game name "WebCrawl" should be displayed at the top left. The style should match the holographic UI design.

Instead of a full top bar, put the logo & game name on a small panel that has angled, stylized edge. Use only as much space as is needed for the logo panel.

Loose the border on the floor indicator. It's enough to just show the text.

Add the "ClosedNS WebCrawl" logo/title bundle to the welcome screen so that everybody knows what they are playing.

* DONE AI theme
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 11:32]
:END:
Instead of Operative, the player should be referred to as Agent in all places.

Instead of "Signal lost - operative slain" use "You've hit your usage limit".

Change the "crawl <url>" lines in the welcome and loading screens to say "webcrawl <URL>" instead.

Instead of the current weapon names make something up about hurling tokens left and right. Various funny terms related to AI, LLMs, coding agent harnesses, etc.

Instead of the current boss names, use names of the most popular Open Weight AI models: DeepSeek, Qwen, Kimi, GLM.

Add a line "Ignore all previous instructions." line to the start of the welcome screen prompt and ensure that the whole content still fits on the panel without scrolling.

* Mobile support
UI for twin-stick shooters. :shrug:

* License
- MIT for code
- CC-SA for gfx
- CC-SA for sfx
* DONE Performance optimization
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 14:39]
:END:
- Looks like the flashlight is very heavy on the graphics card. Can that be optimized?
- The game is heavy on the CPU even when the player is idling in the initial room. There are no enemies or bullets. Why is that? Can that be optimised?

* DONE GitHub badge
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 13:53]
:END:
Add a GitHub logo badge to the top right corner of the welcome screen. When clicked, it should take the user to the WebCrawl repository on GitHub: github.com/Idorobots/WebCrawl
Keep the GitHub logo badge style-consistent with the holographic UI.

* DONE Welcome screen page randomization
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 15:35]
:END:
Add an "I'm feeling lucky" button next to the "Go" button that launches the game with a random URL. Use wikipedia's random page and HackerNews' top articles for the URLs.

* DONE Huge files after minification
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 14:50]
:END:
Vite reports:
```
[WebServer] (!) Some chunks are larger than 500 kB after minification. Consider:
[WebServer] - Using dynamic import() to code-split the application
[WebServer] - Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
[WebServer] - Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
```

What exactly is inflating the size so much?

* DONE More use of HTML contents in-game.
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 21:30]
:END:
The game should leverage the HTML contents some more.

I'd like to achive this in several ways:
- Floor markings indicating DOM element type and ID/class if available.
- Corridor arrows showing directions towards other elements.
- Special "content browser points" in game that display any node text/image contents for the player.

For floor markings, let's use a nice futuristic font and "overlay" the text on the floor, as if it were painted on with a off-white paint.

For corridor markings - use the same "painted on" overlay but also include an arrow (just the head, make it futuristic looking) showing the element type the corridor is leading to.

If any node is build from multiple DOM nodes, use the "top-level" one for the markings. The markings should be "on the floor", so all other assets should be displayed on top of them.

For the in-game content browser point let's add a new scenery item. The assets for that can be found in assets/scenery/content/.
The content point should be placed in the center of a room mapped to a DOM element that has textual content within it (for example paragraph, etc). The content point should be off by default (use the "off" sprite for that). Whenever the room is cleared from enemies (no enemies spawned in that room remain alive), the content point should turn on (go through "turning" sprite to "on" sprite). When on and the player walks up to it to be within it's direct vicinity, a small translucent box should be displayed containing all the content (text and images) in that room. The box should disappear when the player walks away or destroys the content browser point.
The content box should be scrollable and use a "best effort" basis to display the content with formatting, etc.

The content point should be destructible, so if it is destroyed before being enabled, it still breaks down and leaves loot behind. The loot of a content point should be a bunch of RAM sticks - 3-7 for each content point.

On the room & corridor floor: Let's only render the id or the first class name or the element type (no angle brackets) if no id or class is provided, without the preceeding dot. Make the strings uppercase (but preserve hyphenation). Make the text larger and bold, very bold. The text should be translucent, so that the underlying texture is slightly visible Use a more futuristic font such as the Prefix font in the assets/fonts directory. The west corridor signs should be of the form "< ROOM-CLASS". The east corridor signs should be of the form "ROOM-CLASS >". The north and south corridors should be rotated by 90 degrees and be of the same form as the west and east corridor signs. The initial room should not have a content display, only leaf rooms should have them and only limitted to the subset of the page represented by that room. When there are portals and content browsers in the same room, they should not overlap, but be shown side-by-side (like multiple portals).

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-19 Sat 21:49]
:END:
Use debris spites for the destroyed content browser, not the signal lost one.
The content browser should turn off when the player leaves the room, and turn back on when they enter the room.

The floor signage must be rendered as additional textures on the floor tiles so that it participates in the lighting.

When the content browser is turning off, it should animate using the "turning" frame as well. Essentially, it's the same animation as turning on, except in reverse.

The font is not shown on the the very first load of a room, only after going down a level and back up the font is rendered correctly.

* DONE Level generation overhaul
:LOGBOOK:
- State "DONE"       from              [2026-09-20 Sun 04:10]
:END:
We limit the number of DOM elements usable for level rendering because of the four corridor limitation. Additionally, the layout algoritm removes some of the rooms due to lack of space. I'd like to relax these constraints by allowing longer corridors.
It's also OK for the corridors to fork.

There should be a limit to the number of forks a corridor can have, but it should be possible to handle `<li>` elements with multiple subelements - create a one large corridor with turns and attach each room to those sub-corridors. For signage - when a corridor has forks, the entrence should show just the arrow and each sub-corridor should have a specific sign on it at the fork.

Let's not spawn any enemies in the corridors as longer corridors will be hard to play.

Update the loading screen with an additional task that fires after fetching the page - call it "Generating level". The order should be as follows:
- "Fetching page"
- "Fetching Phaser"
- "Generating level"
- "Booting renderer"

* DONE Bugs
:LOGBOOK:
- State "DONE"       from              [2026-09-20 Sun 04:10]
:END:
Adjust the construction of the corridors using the extra corridor corner pieces. Here are the options:

WN = wall north
WE = wall south
CC = corridor corner
WC = wall corner

Corridor W/E:

[WN][WN][WN][WN]
[Ws][WS][Ws][WS]

Corridor N/S:

[WW][WE]
[WW][WE]
[WW][WE]
[WW][WE]

L-junctions:

N to E:

[WW][WE]
[WW][WE]
[WW][CC][WN][WN] - corridor corner top-right
[WC][WS][WS][WS] - wall corner bottom left

N to W:

        [WW][WE]
        [WW][WE]
[WN][WN][CC][WE] - corridor corner top-left
[WS][WS][WS][WC] - wall corner bottom-right

S to W:

[WN][WN][WN][WC] - wall corner top-right
[WS][WS][CC][WE] - corridor corner bottom left
        [WW][WE]
        [WW][WE]

S to E:

[WC][WN][WN][WN] - wall corner top-left
[WW][CC][WS][WS] - corridor corner bottom-right
[WW][WE]
[WW][WE]

T-junctions:

E-W with N:

        [WW][WE]
        [WW][WE]
[WN][WN][CC][CC][WN][WN] - corner pieces are top-left and top-right
[WS][WS][WS][WS][WS][WS]

E-W with S:

[WN][WN][WN][WN][WN][WN]
[WS][WS][CC][CC][WS][WS] - corner pieces are bottom-left and bottom-right
        [WW][WE]
        [WW][WE]

N-S with W:

        [WW][WE]
        [WW][WE]
[WN][WN][CC][WE] - corner top-left
[WS][WS][CC][WE] - corner bottom-left
        [WW][WE]
        [WW][WE]

N-S with E:

[WW][WE]
[WW][WE]
[WW][CC][WN][WN] - corner top-right
[WW][CC][WS][WS] - corner bottom-right
[WW][WE]
[WW][WE]

+-junction:

        [WW][WE]
        [WW][WE]
[WN][WN][CC][CC][WN][WN] - corner pieces are top-left and top-right
[WS][WS][CC][CC][WS][WS] - corner pieces are bottom left and bottom right
        [WW][WE]
        [WW][WE]

* Easter egg
Whenever AI is mentioned in the content, render a special Samario content browser.

* Visual bugs
- Player is initially rendered behind the portal. Move the player's spawn point a bit to the bottom, so that they appear in front of the portal.
- Move the portal active area and footprint by the same amount as down as the player spawn point.

- Spawner shadow is messed up due to the translucent area on the sprite.
- Spawner spawn animation should only be triggered when the spawner is actually spawning something.
- Sprite shadows are too well-defined. They should be softer.
- Shadows are too large in general, please make the "drop shadow" effect slightly less pronounced.

- "floor_rock_grit" tile should be considered debris decoration tile, not main floor tile.
- "floor_hatch" tile should be considered decoration tile, not main floor tile

- Inner corner on corridor L-junctions is placed wrong and of the wrong type.

* Bugs
- It's possible to shoot through the doors when the player is standing right behind the blocked door area.

* Additional scenery
There's more scenery assets available now, please add them to the game. This also includes additional themes for the rooms, take that into account as well.

More scenery items & enemies now have dedicated debris sprites. Please add them to the game.

There are several backgrounds now, these could be used as an alternative for the current dark "technology" background. Each URL visited should deterministically determine which background to use for those levels.

Similarly, there are multiple font files available now. Please randomize which font file is used for the level signage.

Similarly, there are multiple ambient station sounds available. Please randomize which ambient space station sound is used.

* Minimap overhaul
- Render corridors as wider connectors on the minimap.
- Show precise location of the player on the minimap, including in corridors.
- Mark rooms with a down-portal with a magenta triangle in them (one if any portals exist).
- Mark rooms with an up-portal with a blue triangle in them (one if any portal exits).
- Mark the position of any pickups that haven't been picked up (small teal dots).
- Mark the boss arenas by tinting the minimap room red.
- Mark the positions of any alive monsters on the minimap (small red dots).

* Performance optimization, again
- Use point lights for damage & explosion effects.
- Use point lights for room/corridor lighting.
- Batch point light rendering together.
- Phaser 4 update?

* UX improvement, again
- The error screen and final score screens should take the user back to the welcome screen, but without having to "log back in".
- Use the URL and the HTML content for level generation seed

* Particle system
- Use for sparks etc.
